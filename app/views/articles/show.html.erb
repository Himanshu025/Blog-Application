<p>
	<b>Title:</b>
	<%= @article.title %>
</p>

<p>
	<b>Text:</b>
	<%= @article.text %>
</p>

<h2><%= link_to 'Comments' ,  article_comments_path(@article) %></h2> 
<%= render @article.comments %>


<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>